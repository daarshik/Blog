// "use client";

// import dynamic from "next/dynamic";

// // Dynamically import the Editor component with SSR disabled
// const Editor = dynamic(() => import("./Blog"), { ssr: false });

// const page = () => {
//   return <Editor />;
// };

// export default page;
